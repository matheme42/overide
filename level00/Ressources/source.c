#include <stdlib.h>
#include <stdio.h>

bool main(int ac, char **argv)
{
    int number;
    
    puts("***********************************");
    puts("*            -Level00 -           *");
    puts("***********************************");
    puts("Password:");
    number = atoi(argv[0])
    if (number != 5276 {
        puts("\nInvalid Password!");
    } else {
        puts("\nAuthenticated!");
        system("/bin/sh");
    }
    return number != 5276;
}
